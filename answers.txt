What is the difference between a merge and a rebase?
git merge combines changes made in both branches into one new commit.
rebase takes a target branch and applies new commits on top of it,
in order to maintain changes.